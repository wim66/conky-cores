--[[
conky-cores
by @wim66
]]

conky.config = {
    alignment = 'top_left',
    gap_x = 302,
    gap_y = 75,
    -- Rough starting point for an 8-core CPU (2 columns x 4 rows).
    -- Turn on CFG.debug_show_canvas in widget.lua to check whether this
    -- is tall enough for YOUR core count -- more cores need more height,
    -- fewer cores need less. See the readout it draws on-screen.
    minimum_width = 340,
    minimum_height = 700,
    maximum_width = 340,

    use_xft = true,
    override_utf8_locale = true,

    no_buffers = true,
    double_buffer = true,
    update_interval = 1,
    total_run_times = 0,
    cpu_avg_samples = 2,

    background = true,
    own_window = true,
    own_window_type = 'normal',
    own_window_class = 'Conky',
    own_window_title = 'conky-cores',
    own_window_argb_visual = true,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    lua_load = './widget.lua',
    lua_draw_hook_post = 'main',
};

conky.text = [[

]]
